// Next

// Local Components
// import { withAuth } from '@/auth/withAuth';
import { Layout } from '@/components';

// Styles
import s from './styles/Botiquines.module.scss';

function Botiquines() {
  return (
    <Layout pageTitle="Inicio">
      <h1>Botiquines</h1>
    </Layout>
  );
}

export default Botiquines;
