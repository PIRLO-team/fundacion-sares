// Next

// Local Components
// import { withAuth } from '@/auth/withAuth';
import { Layout } from '@/components';

// Styles
import s from './styles/Solicitudes.module.scss';

function Solicitudes() {
  return (
    <Layout pageTitle="Inicio">
      <h1>Solicitudes</h1>
    </Layout>
  );
}

export default Solicitudes;
