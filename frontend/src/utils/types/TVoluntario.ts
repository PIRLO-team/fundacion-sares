export type TVoluntario = {
  is_active: boolean;
  created_date?: string;
  direct_volunteer_id: string;
  first_name: string;
  last_name: string;
  email: string;
  profession: string;
  document: string;
  phone: string;
  other_contact: string;
  observation: string;
};
